/nagipath

# Local development output and an accidental browser screenshot artifact.
/tmp/
/--full-page

# The Tailwind standalone CLI, fetched by `make css`. The CSS it generates
# (internal/web/static/app.css) IS committed — that is what the binary embeds.
/tools/

*.db
*.db-shm
*.db-wal

# Generated by testlab/gen.sh. A private key in a repository is a private key on
# every laptop that clones it, lab or not.
testlab/keys/
testlab/certs/
